// var UserView = require('./view-user');

module.exports = Backbone.View.extend({
	el: 'header',
	initialize: function(){
		
		// var userView = new UserView({
		// 	el: this.$el.find('#user')
		// });
	}
});